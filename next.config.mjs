/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // We can add any webpack custom configs here if needed, but standard is perfectly fine.
};

export default nextConfig;
